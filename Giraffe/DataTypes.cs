using System;

namespace PartialClasses
{
    public partial class PartialClass
    {
        public void DataTypes()
        {

            string phrase = "Giraffe Academy";
            char grade = 'C';
            int age = 10;
            // Precision order float<double<decimal
            // can use decimal when you are handling values which you would like to be exremely precise
            double gpa = 3.0;
            bool isMale = false;

            // Printing out a constant. We can do the same for boolean
            Console.WriteLine(30);

            Console.ReadLine();


        }
    }
}